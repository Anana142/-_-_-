<Page x:Class="ООО_СТОЛОВЫЕ_ПРИБОРЫ.View.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespase:ООО_СТОЛОВЫЕ_ПРИБОРЫ.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label Content="Авторизация" HorizontalAlignment="Center" FontSize="25" FontWeight="Bold"/>
        <Label Content="Логин: " Width="120" Margin="10"/>
        <TextBox Width="120" Margin="10"/>
        <Label Content="Пароль: " Width="120" Margin="10"/>
        <PasswordBox Width="120" Margin="10" x:Name="pass"/>
        <Button Width="100" Height="35" Margin="10" Content="Войти" Command="{Binding Enter}"/>


    </StackPanel>
</Page>
